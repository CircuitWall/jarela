{
  "name": "jarela-oauth-proxy",
  "version": "1.0.0",
  "description": "Server-side OAuth client_secret injection proxy for Jarela Gmail integration.",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": "22"
  },
  "scripts": {
    "start": "functions-framework --target=tokenProxy --port=8080"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0"
  }
}
